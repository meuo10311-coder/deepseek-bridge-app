plugins {
    id "com.android.application"
}

android {
    namespace "com.agent.bridge"
    compileSdk 35

    defaultConfig {
        applicationId "com.agent.bridge"
        minSdk 24
        targetSdk 35
        versionCode 1
        versionName "1.0"
    }
}
